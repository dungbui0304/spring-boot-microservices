services:
  mongodb: 
    image: mongo:7.0.5
    container_name: mongodb
    ports: 
      - "27017:27017"
    environment:
      MONGO_URI: mongodb://root:password@mongodb:27017/product-service?authSource=admin
    volumes:
      - ./data:/data/db